import React from 'react';
import useStyles from "../../common/styles/style";

const AdvertisingComponents = () => {
    const {advetisingBlock} = useStyles();
    return (
        <div className={advetisingBlock}>

        </div>
    );
};

export default AdvertisingComponents;
